name: functions
description: Firebase Functions for Play Integrity API verification

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  firebase_functions: ^4.0.0
  http: ^1.1.0

dev_dependencies:
  test: ^1.24.0
